var keyMirror = require('react/lib/keyMirror');

// Define actions
module.exports = keyMirror({
    TOGGLE_NETWORK: null,
    SET_URL: null,
    SET_TEXT: null,
    CHANGE_SIZE: null,
    TOGGLE_ICON: null
});
